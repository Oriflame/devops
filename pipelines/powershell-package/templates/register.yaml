parameters:
  pathToProjects: '$(Build.SourcesDirectory)/src/'

steps:
- powershell: Import-Module ./*.psd1
  displayName: Register Powershell Module
  workingDirectory: ${{ parameters.pathToProjects }}